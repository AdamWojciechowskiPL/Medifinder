{
  "name": "medifinder-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step for static site'",
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}